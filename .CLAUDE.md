# Claude Forward - 项目文档

## 项目概述

Claude Forward 是一个自托管的远程编程系统，允许用户通过手机或其他设备远程控制家中内网电脑上运行的 Claude Code。

**架构**：
```
手机端 (Web UI) <--WebSocket--> 云服务器 (Server) <--WebSocket--> 本地电脑 (Client)
```

**核心场景**：用户在手机上发消息，Client 端调用本地 Claude CLI (`claude -p`) 执行，结果流式返回 Web UI。当 Claude 需要执行敏感操作（如 `rm`），Hook 拦截并通过 WebSocket 转发到 Web UI，用户在手机上审批。

## 技术栈

- **语言**: Go 1.21
- **核心依赖**: gorilla/websocket, creack/pty, gopkg.in/yaml.v3, google/uuid
- **前端**: 单文件 HTML/CSS/JS (web/index.html)，使用 marked.js 渲染 Markdown
- **终端**: tmux + PTY 转发（终端模式，旧），Claude CLI `--output-format stream-json`（聊天模式，当前主流）
- **权限系统**: Claude PreToolUse Hook + 本地 HTTP Server + WebSocket 转发审批

## 项目结构

```
claude-forward/
├── cmd/
│   ├── server/main.go          # 服务器入口：HTTP/WS 服务、TLS、静态文件、gzip
│   └── client/main.go          # 客户端入口：信号处理、tmux 重复检测、生命周期
├── internal/
│   ├── protocol/message.go     # 消息协议：所有 MessageType 常量和 Payload 结构体
│   ├── server/
│   │   ├── hub.go              # 连接管理：clients/users map、attachMap、注册/注销
│   │   ├── handler.go          # WebSocket 消息处理：读写协程、消息路由
│   │   ├── config.go           # 服务器配置结构体和加载
│   │   └── auth.go             # Token 认证（Header/URL 参数）
│   └── client/
│       ├── connection.go       # 客户端核心：WebSocket 连接、重连循环、消息处理、权限系统集成
│       ├── claude.go           # Claude CLI 管理：JSONL 解析、流式事件、会话恢复、--settings 支持
│       ├── hook_server.go      # Hook 服务器：PreToolUse Hook 拦截、权限请求转发、超时处理
│       ├── permissions.go      # 权限规则解析：读取 ~/.claude/settings.json 的 allow/deny/ask 规则
│       ├── tmux.go             # tmux/PTY 管理：会话创建、Attach、Resize、Capture
│       └── config.go           # 客户端配置：从路径推导 ID/Name/SessionName、配置文件搜索
├── web/
│   ├── index.html              # Web UI（单文件 SPA，含权限审批弹窗）
│   └── lib/                    # xterm.js 及插件（终端模式用）
├── configs/
│   ├── server.yaml.tpl         # 服务器配置模板
│   └── client.yaml.tpl         # 客户端配置模板
├── deploy/
│   ├── install.sh              # 一键部署脚本（交互式配置、systemd 安装）
│   └── claude-forward-server.service  # systemd 服务单元
├── start-server.sh             # 开发用服务器启动脚本
├── start-client.sh             # 开发用客户端启动脚本（build 后自动同步到 ~/.claude-forward/）
└── Makefile                    # 构建目标：build-server, build-client, install-client, gen-cert, deps
```

## 消息协议

定义在 `internal/protocol/message.go`。所有 WebSocket 消息为 JSON 格式：

```json
{
  "type": "message_type",
  "from": "sender_id",
  "to": "receiver_id",
  "payload": { ... }
}
```

### 消息类型

| 类型 | 方向 | 说明 |
|------|------|------|
| `register` / `ack` | Client → Server | 客户端注册 |
| `list` | User → Server | 列出在线客户端 |
| `attach` / `attached` | User ↔ Server | 用户附加到客户端 |
| `detach` / `detached` | User ↔ Server | 用户断开连接 |
| `input` / `output` / `resize` | User ↔ Client | 终端 I/O 转发（旧模式） |
| `chat_input` | User → Client | 聊天消息输入 |
| `chat_ack` | Client → User | 确认收到聊天消息 |
| `chat_message` | Client → User | Claude 结构化输出事件 |
| `chat_ready` | Client → User | Claude 处理完毕 |
| `chat_error` | Client → User | 处理错误 |
| `permission_request` | Client → User | 工具权限审批请求 |
| `permission_response` | User → Client | 权限审批结果 |
| `new_session` | User → Client | 开始新 Claude 会话 |
| `kill_session` | User → Client | 销毁 tmux 会话 |
| `session_info` | Client → User | 会话元数据 |
| `ping` / `pong` | 双向 | 心跳（30s 间隔） |

### 聊天事件类型 (`chat_message.event_type`)

- `init` - 会话初始化（含 session_id）
- `stream_delta` - 流式文本片段
- `text` - 完整文本块
- `thinking` - 思考过程
- `tool_start` / `tool_end` - 工具调用
- `result` - 最终结果（含 `cost_usd`、Token 用量）
- `user_message` - 用户发送的消息（存入 sessionEvents 用于回放）

## 核心组件

### Server (`internal/server/`)

**Hub** (`hub.go`)：连接管理中心
- 维护 `clients` map（已注册的本地客户端）和 `users` map（用户连接）
- `attachMap` 记录 user → client 映射关系
- 通过 channel 驱动的注册/注销事件循环
- **指针比较**：unregister 时使用 `existing == conn` 指针比较，防止旧连接注销时误删同 ID 的新连接（修复竞态）
- 客户端断开时自动通知所有附加的用户

**Handler** (`handler.go`)：WebSocket 消息路由
- 每个 WS 连接启动 readPump + writePump 两个协程
- Token 认证通过 URL 参数 `?token=xxx`
- 心跳：30s ping 间隔，60s 读超时
- 消息路由：根据 `attachMap` 在 user ↔ client 之间转发
- **客户端切换通知**：`handleAttach` 时检查用户是否从其他客户端切换，通知旧客户端 detach
- `readLimit`: 512KB
- 聊天/权限消息复用 `handleChatClientToUser` 和 `handleChatUserToClient` 进行转发

**Auth** (`auth.go`)：简单 Token 验证，支持 Header 和 URL 参数

### Client (`internal/client/`)

**Connection** (`connection.go`)：客户端核心
- 重连循环：断开后按 `reconnect_interval` 自动重连
- 注册时发送客户端信息（ID、Name、PID、Path）
- `startForwarding()`：PTY 输出 → WebSocket 转发
- `handleChatInput()`：调用 Claude CLI 并流式转发事件
- `initPermissionSystem()`：启动 Hook Server 和权限检查器
- 追踪 `attachedUser`（当前连接的 Web UI 用户），用于权限请求的目标路由
- 原子操作 `connClosed` 防止重复关闭
- **会话事件持久化**：`sessionEvents` 缓冲区存储当前会话所有事件，按 clientID 持久化到 `~/.claude-forward/session_events_<clientID>.json`
- **断线重连回放**：用户重新 attach 时，按快照 → 增量 → setUser 三步回放事件，避免竞态
- `sentUpTo` 去重机制：用户离线期间事件仅缓冲不标记已发送，重连后完整回放

**ClaudeManager** (`claude.go`)：Claude CLI 进程管理
- 调用 `claude -p <text> --output-format stream-json --verbose`
- 权限绕过：`--dangerously-skip-permissions` + `--setting-sources ""`（阻止加载用户 deny 规则）
- `injectUserEnv()`：从 `~/.claude/settings.json` 读取 env 配置注入到 Claude 进程环境（因为 `--setting-sources ""` 阻止了 Claude 自行加载，需手动注入 API 认证等环境变量）
- 支持 `--resume <session_id>` 会话恢复，session_id 按 clientID 持久化到 `~/.claude-forward/session_id_<clientID>`
- 支持 `--settings <path>` 传入 Hook 配置文件
- 支持 `--allowedTools`, `--max-turns` 参数
- JSONL 逐行解析：system/init、assistant/content_blocks、content_block_delta/start/stop、result
- Scanner buffer: 10MB（Claude 输出行可能很长）
- 日志重定向到 `/tmp/claude-forward-claude-<clientID>.log`（按客户端区分）
- `Abort()` 通过 context cancel 终止进程
- `ClaudeConfig` 新增 `ClientID` 字段，用于区分多客户端文件路径

**HookServer** (`hook_server.go`)：PreToolUse Hook 处理
- 监听 `127.0.0.1` 随机端口的 HTTP 服务器
- `POST /hook/pre-tool-use` 接收 Claude Hook 调用
- 权限流程：allow → HTTP 200，deny → HTTP 403，ask → WebSocket 转发到 Web UI
- `pending map[string]chan bool` 跟踪等待中的审批请求
- 超时 60s 自动拒绝（安全默认）
- `GenerateSettingsFile()` 生成 JSON 文件（持久化到 `~/.claude-forward/hooks-settings-<clientID>.json`），配置 PreToolUse Hook
- `loadUserSettings()` 读取用户 `~/.claude/settings.json` 保留 env/model/language/apiProvider 配置
- Hook 命令：`curl -sf -X POST http://127.0.0.1:PORT/hook/pre-tool-use -d @- && exit 0 || (echo "Permission denied"; exit 2)`
- settings 文件使用持久化目录 `~/.claude-forward/`（非 /tmp），避免被 macOS 清理

**PermissionChecker** (`permissions.go`)：权限规则解析
- 从 `~/.claude/settings.json` 加载 `permissions.allow/deny/ask` 规则
- 规则格式：`Bash(rm *)` → `{Tool: "Bash", Pattern: "rm *"}`
- `Check()` 方法返回 `allow`/`deny`/`ask`
- `extractMatchValue()` 提取匹配字段：Bash → command，其他 → file_path
- `matchGlob()` 支持 `*` 通配符匹配
- 未匹配任何规则时默认 `allow`（配合 bypassPermissions 使用）

**TmuxManager** (`tmux.go`)：tmux + PTY 终端管理
- `EnsureSession()`：确保 tmux 会话存在，失败时 kill-server 后重试
- `Attach()`：通过 `tmux attach` + PTY 连接，支持进程存活检测
- `CaptureOutput()`：`tmux capture-pane -p -e` 获取当前屏幕（含 ANSI 转义）
- `Resize()`：PTY 窗口大小调整
- 无 tmux 时降级为直接 shell + PTY

**Config** (`config.go`)：智能默认值 + 配置文件搜索
- `ResolveConfigPath()`：按优先级查找配置文件（显式指定 > `~/.claude-forward/client.yaml` > `configs/client.yaml` > 默认值）
- `ApplyDefaults()`：从工作目录路径推导 client ID/Name 和 tmux session name（仅当配置文件中对应字段为空时）
- Session name 格式：`cf-<sanitized_basename>`
- Client ID 格式：`<hostname>-<basename>`
- tmux 名清洗：`.` `:` 替换为 `-`，折叠重复 `-`，截断 50 字符

### Web UI (`web/index.html`)

单文件 SPA，仿 Claude.ai 官网风格设计：
- **连接页**：服务器地址 + Token 输入，支持 URL 参数自动填充，清除本地记录按钮
- **客户端列表**：显示在线客户端（名称、PID、路径）
- **聊天视图**：
  - 用户消息气泡（右对齐，赤陶色）
  - 助手消息（左对齐，Markdown 渲染）
  - 思考过程（斜体，琥珀色边框）
  - 工具调用卡片（可折叠，显示工具名和参数）
  - 流式输出动画（三柱脉冲）
  - 费用显示、上下文 Token 用量、本轮输出 Token
  - 发送后即时显示"发送中..."占位气泡
- **权限审批弹窗**：
  - 毛玻璃背景 + 居中卡片
  - 显示工具名称、输入参数预览
  - 60s 倒计时（≤10s 时变红警告）
  - 批准（绿色）/ 拒绝按钮
  - 超时自动拒绝
- **持久化**：聊天记录保存在 localStorage（`cf_history_<clientId>`），支持从 localStorage 重建聊天 DOM
- **断线重连**：自动 3s 重连，恢复 attach 状态，智能历史恢复（按 session_id 匹配）
- **断线重连**：自动 3s 重连，恢复 attach 状态

## 权限审批数据流

```
Claude CLI 执行工具 → PreToolUse Hook (curl POST stdin)
  → Hook Server 接收 → PermissionChecker 检查规则
    → allow 规则: HTTP 200 → Claude 执行
    → deny 规则: HTTP 403 → Claude 拒绝执行
    → ask 规则: WebSocket permission_request → Server 转发 → Web UI 弹窗
      → 用户批准: permission_response(approved=true) → Server 转发 → HTTP 200
      → 用户拒绝: permission_response(approved=false) → HTTP 403
      → 超时(60s): HTTP 403 自动拒绝
```

关键技术细节：
- Hook 命令通过 `--settings` 标志注入给 Claude CLI（临时 JSON 文件，不修改用户全局设置）
- `curl -sf` 的 `-f` 标志让 HTTP 403 映射为 curl exit 22，`|| exit 2` 映射为 Hook deny
- Hook Server 在 Client 启动时初始化（`initPermissionSystem()`），绑定 `attachedUser` 用于路由
- 无用户连接时，ask 规则超时后自动拒绝（安全默认）

## 配置

### 服务器 (`configs/server.yaml.tpl`)

```yaml
server:
  host: "0.0.0.0"
  port: 6022
  tls:
    enabled: true
    cert_file: ""          # 留空自动生成自签名
    key_file: ""
    domain: ""             # 预留 Let's Encrypt
auth:
  tokens: ["secret"]       # Token 列表
session:
  timeout: 300             # 秒
  max_clients: 10
logging:
  file: "/var/log/claude-forward/server.log"
  max_days: 7
  log_level: "info"
```

### 客户端 (`~/.claude-forward/client.yaml` 或 `configs/client.yaml`)

```yaml
server:
  url: "wss://server:6022"
  token: "secret"
  reconnect_interval: 5
client:
  id: ""                   # 留空自动推导为 hostname-basename
  name: ""                 # 留空自动推导为项目目录名
tmux:
  session_name: ""         # 留空自动推导为 cf-basename
  auto_start: true
  shell: ""
```

配置文件搜索顺序：`-config` 参数 > `~/.claude-forward/client.yaml` > `configs/client.yaml` > 默认值。

### Claude 相关配置（硬编码在 `ClaudeConfig` 中）

```go
// ClaudeConfig 由程序内部设置，不从 YAML 读取
Path             string  // claude 二进制路径，默认 "claude"
AllowedTools     string  // 允许的工具列表（如 "Bash,Read,Edit,Write,Glob,Grep"）
MaxTurns         int     // 最大轮次（如 50）
HookSettingsPath string  // Hook 配置文件路径（程序运行时生成）
ClientID         string  // 客户端唯一标识（用于区分多客户端文件路径）
```

这些值目前在 `configs/client.yaml` 中通过 `claude` 段配置，实际运行配置参考部署目录。

## 构建

```bash
make build            # 构建 server + client
make build-server     # 仅构建 server（含 ldflags 版本信息）
make build-client     # 仅构建 client（含 ldflags 版本信息）
make install-client   # 编译 client + 安装到 ~/.claude-forward/ + 创建 /usr/local/bin/cf
make gen-cert         # 生成自签名证书
make deps             # 下载依赖
```

构建产物输出到 `bin/` 目录。Server 和 Client 均通过 ldflags 注入 `buildInfo`（版本号+时间）。

## 部署

### 服务器（云服务器）
```bash
sudo ./deploy/install.sh    # 一键部署：交互式配置 + systemd 安装
```

### 客户端（本地电脑）

**首次安装：**
```bash
cd claude-forward
make install-client                  # 编译 + 安装到 ~/.claude-forward/ + 创建 cf 命令
sudo ln -sf ~/.claude-forward/client /usr/local/bin/cf  # 如果上一步提示权限不足
vim ~/.claude-forward/client.yaml    # 编辑服务器地址和 Token
```

**日常使用：**
```bash
cf                        # 在任意项目目录下启动，自动加载 ~/.claude-forward/client.yaml
cf -dir ~/my-project      # 指定工作目录
cf -version               # 查看版本
```

**开发迭代（在项目目录下）：**
```bash
./start-client.sh restart    # 编译 + 自动同步到 ~/.claude-forward/ + 重启
```

`start-client.sh` 每次 build 后会自动将二进制同步到 `~/.claude-forward/client`，确保 `cf` 命令始终是最新版。

## 双模式架构

项目同时支持两种使用模式：

1. **终端模式**（传统）：tmux + PTY + xterm.js，完整的终端转发
2. **聊天模式**（当前主流）：Claude CLI `stream-json` + 结构化消息协议 + 聊天 UI

聊天模式通过 `chat_input` / `chat_message` / `chat_ready` 消息类型实现，Client 端 `ClaudeManager` 解析 Claude 的 JSONL 输出，Server 端透传消息。

## 重要实现细节

- **TUI 不花屏**：Claude CLI 必须使用 stdio 继承，不能用 PTY 手动复制
- **日志隔离**：Claude 进程的 stderr 重定向到 `/tmp/claude-forward-claude.log`，避免干扰
- **重复客户端检测**：`cmd/client/main.go` 启动时检查 tmux session 是否已存在
- **WebSocket 认证**：URL 参数 `?token=xxx`，所有来源允许跨域
- **gzip 压缩**：Server 对 `.js/.css/.html/.json/.svg/.md` 静态资源启用 gzip
- **自签名证书**：Server 启动时内存中自动生成 RSA 2048 证书，有效期 365 天
- **即时反馈**：Web UI 发送消息后立即显示"发送中..."占位气泡，收到实际响应后替换
- **构建信息注入**：Server 和 Client 均通过 ldflags 注入版本和时间，避免 sed 修改 HTML 导致 auto-upgrade 失败
- **客户端全局安装**：`make install-client` 安装到 `~/.claude-forward/` + symlink `/usr/local/bin/cf`，从任意目录直接 `cf` 启动
- **配置文件自动搜索**：`ResolveConfigPath()` 按优先级查找配置（显式 > `~/.claude-forward/client.yaml` > `configs/client.yaml` > 默认值）
- **开发同步**：`start-client.sh` build 后自动同步到 `~/.claude-forward/client`，保持 `cf` 命令最新

## 关键路径

| 场景 | 代码路径 |
|------|----------|
| 用户发消息 | Web UI `sendChatMessage()` → WS `chat_input` → Server 透传 → Client `handleChatInput()` → `ClaudeManager.SendMessage()` → JSONL 解析 → WS `chat_message` 事件流 → Server 透传 → Web UI `handleChatMessage()` |
| 工具权限审批 | Claude CLI PreToolUse → curl POST → `HookServer.handlePreToolUse()` → `PermissionChecker.Check()` → ask 规则 → WS `permission_request` → Server 透传 → Web UI `showPermissionDialog()` → 用户操作 → WS `permission_response` → Server 透传 → `HookServer.HandleResponse()` → HTTP 200/403 |
| 终端模式 | Web UI input → WS `input` → Server 透传 → Client `tmux.Write()` → PTY → tmux → PTY output → `startForwarding()` → WS `output` → Server 透传 → Web UI xterm |
| 客户端注册 | Client `Connect()` → WS `register` → Server `handleRegister()` → 清理 users map 旧条目 → 注册到 clients map → WS `ack` |
| 断线重连 | Client: `Run()` 重连循环 + `reconnect_interval`。Web UI: `ws.onclose` → 3s 重连 + 恢复 attach 状态。Client: 重连后回放 sessionEvents 快照 + 增量事件 |
| 多客户端隔离 | session_id/session_events/hooks-settings/log 文件均按 clientID 区分，存储在 `~/.claude-forward/` 目录 |

## 技术决策记录

### 决策 1：聊天模式替代终端转发
- **背景**：终端转发（tmux + PTY + xterm.js）在手机上体验差，且 Claude CLI 在非交互模式下无法启动 TUI
- **决策**：使用 `claude -p --output-format stream-json` 的非交互模式，解析 JSONL 输出，构建聊天 UI
- **结果**：更适合手机端使用，结构化数据便于展示

### 决策 2：PreToolUse Hook 实现权限审批
- **背景**：Claude CLI `-p` 模式下 `ask` 规则的工具被自动拒绝，无法交互式确认
- **决策**：利用 `--settings` 标志注入 PreToolUse Hook，本地启动 HTTP Server 拦截工具调用，通过 WebSocket 转发到 Web UI 审批
- **额外**：使用 `--dangerously-skip-permissions` + `--setting-sources ""` 完全绕过 Claude 内部权限系统，通过 Hook 自行实现权限控制
- **env 注入**：`injectUserEnv()` 手动读取 `~/.claude/settings.json` 中的 env 并注入到 Claude 进程，因为 `--setting-sources ""` 阻止了 Claude 自行加载
- **原因**：Hook 是 Claude Code 原生支持的拦截机制，干净且不影响用户全局设置
- **备选方案**：直接覆盖 `~/.claude/settings.json`（侵入性大）、stdin 交互（`-p` 模式不支持）
- **状态**：已实现

### 决策 3：Hook 退出码映射
- **方案**：`curl -sf -X POST URL -d @- && exit 0 || (echo "Permission denied"; exit 2)`
- **原因**：`-f` 让 HTTP 4xx/5xx 映射为 curl 非 0 退出码（22），`||` 捕获并映射为 exit 2（Claude Hook deny 码）
- **结果**：HTTP 200 → 批准，HTTP 403 → 拒绝，连接失败 → 拒绝（安全默认）

### 决策 4：临时文件而非修改全局设置
- **方案**：使用 `--settings` 传入临时生成的 JSON 文件
- **原因**：不修改用户的 `~/.claude/settings.json`，进程退出时自动清理临时文件

## 已完成功能

- [x] WebSocket 双向通信（Server 透传消息）
- [x] 客户端注册与自动重连
- [x] 聊天式 UI（仿 Claude.ai 官网风格）
- [x] Claude CLI 流式输出解析（JSONL → 结构化事件）
- [x] 会话恢复（`--resume`）
- [x] Markdown 渲染（marked.js）
- [x] 工具调用卡片（可折叠）
- [x] 思考过程显示
- [x] 费用统计
- [x] 聊天记录持久化（localStorage）
- [x] 断线重连（客户端 + Web UI）
- [x] 发送消息即时反馈（"发送中..."占位气泡）
- [x] iOS Safari 兼容（viewport-fit=cover + safe-area-inset）
- [x] 构建信息 ldflags 注入（Server + Client）
- [x] 部署脚本 + systemd 服务
- [x] PreToolUse Hook 权限拦截系统
- [x] 权限规则解析（~/.claude/settings.json）
- [x] Web UI 权限审批弹窗（倒计时 + 批准/拒绝）
- [x] Server 端权限消息路由
- [x] 上下文 Token 用量显示（含 cache_creation/cache_read）
- [x] Client 重启后自动恢复会话历史（--resume + session_id 持久化）
- [x] 会话事件持久化（sessionEvents 按 clientID 存储）
- [x] 多客户端文件隔离（session_id/events/hooks/log 按 clientID 区分）
- [x] Hub unregister 指针比较（修复连接注销竞态）
- [x] 客户端切换时通知旧客户端 detach
- [x] 断开重连后聊天记录去重
- [x] 清除本地记录按钮
- [x] --dangerously-skip-permissions + --setting-sources "" 权限绕过
- [x] injectUserEnv() 注入 API 认证等环境变量
- [x] Hook settings 文件持久化到 ~/.claude-forward/（避免 macOS 清理）
- [x] 客户端全局安装（make install-client + cf 命令）
- [x] 配置文件自动搜索（ResolveConfigPath）
- [x] start-client.sh build 后自动同步已安装的 client
- [x] 客户端 -version 参数支持

## 待办事项

### 高优先级
- [ ] **错误处理增强**：Hook Server 崩溃时的降级策略（当前默认拒绝，可考虑重试）
- [ ] **日志持久化**：Client 端日志输出到文件而非仅 stdout

### 中优先级
- [ ] **多会话管理**：支持同一客户端运行多个 Claude 会话
- [ ] **会话列表**：Web UI 展示历史会话，支持切换
- [ ] **消息编辑/重试**：编辑已发送的消息或重新生成回复
- [ ] **权限规则热更新**：运行时重新加载 settings.json 而不重启客户端

### 低优先级
- [ ] **文件浏览器**：Web UI 浏览客户端文件系统
- [ ] **代码高亮**：工具输出中的代码语法高亮
- [ ] **消息搜索**：搜索聊天记录
- [ ] **多语言支持**：Web UI 国际化
- [ ] **图片支持**：发送/接收图片（Claude Vision）
